#include <iostream>
#include <map>
#include <string>
#include <vector>

using std::map;
using std::string;
using std::vector;

map<string, vector<int>> crossLink(istream&, vector<string> searchWords(const string&) = wordSeparator);